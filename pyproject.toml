[project]
name = "csv_bundle_fqm"
version = "0.1.0"
description = "Simple project to filter, query, merge across CSV files."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.3.0",
    "jupyterlab>=4.4.3",
    "pandas>=2.3.0",
    "streamlit>=1.45.1",
]
